server:
  port: 8080

spring:
  application:
    name: greeting-service

  cloud:
    config:
      enabled: false

greeting-service:
  greeting: "Pronto"
  default-value: "Mundo"

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
